import { NextResponse } from 'next/server';
import { slugFromUrl } from '@/lib/demo-data';
export const runtime='nodejs';
function normalizeUrl(value:string){const trimmed=value.trim(); if(!trimmed)return ''; return trimmed.startsWith('http://')||trimmed.startsWith('https://')?trimmed:`https://${trimmed}`;}
async function readUrl(request:Request){ const contentType=request.headers.get('content-type')||''; if(contentType.includes('application/json')){const body=await request.json().catch(()=>null); return typeof body?.url==='string'?body.url:'';} const formData=await request.formData().catch(()=>null); const url=formData?.get('url'); return typeof url==='string'?url:''; }
export async function POST(request:Request){ const rawUrl=await readUrl(request); const url=normalizeUrl(rawUrl); if(!url) return NextResponse.json({error:'A website URL is required.'},{status:400}); const reportId=slugFromUrl(url); const accept=request.headers.get('accept')||''; if(accept.includes('text/html')) return NextResponse.redirect(new URL(`/reports/${reportId}?url=${encodeURIComponent(url)}`,request.url),303); return NextResponse.json({ok:true,reportId,url,redirectTo:`/reports/${reportId}?url=${encodeURIComponent(url)}`}); }

import { redirect } from 'next/navigation';
import { ArrowRight, Search } from 'lucide-react';
import { AnalyzeForm } from '@/components/audit/analyze-form';
import { AppShell } from '@/components/layout/app-shell';
import { createClient } from '@/lib/supabase/server';
export default async function DashboardPage(){ const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user) redirect('/auth/login?redirect=/dashboard'); const {data:profile}=await supabase.from('profiles').select('plan, subscription_status, audits_this_month').eq('id',user.id).single(); const plan=profile?.plan??'free'; return <AppShell title="Dashboard" subtitle="Run audits and review UX, WCAG, accessibility, and conversion findings"><section className="card app-section"><div className="score-shell"><div className="score-ring" aria-label="UX score 72 out of 100"><div className="score-ring-inner">72</div></div><div><p className="app-kicker">UX Score</p><h2 className="mt-3 text-5xl font-semibold tracking-tight text-white">Good, but losing conversions.</h2><p className="mt-4 app-muted">Your current audit profile shows opportunities around CTA clarity, accessibility, and conversion hierarchy.</p><div className="score-metric-grid">{[['Usability','68'],['Accessibility','54'],['Conversion','61'],['Visual Design','80']].map(([label,score])=><div key={label} className="score-metric-card"><span>{label}</span><strong>{score}</strong></div>)}</div></div></div></section><div className="app-grid-3 mt-5"><div className="card app-stat"><p className="app-stat-label">Current plan</p><p className="app-stat-value">{plan==='pro'?'Pro':'Free'}</p><p className="app-muted">{profile?.subscription_status??'inactive'}</p></div><div className="card app-stat"><p className="app-stat-label">Audits this month</p><p className="app-stat-value">{profile?.audits_this_month??0}</p><p className="app-muted">Audit history and limits refresh monthly.</p></div><div className="card app-stat"><p className="app-stat-label">Report type</p><p className="app-stat-value" style={{fontSize:'30px'}}>{plan==='pro'?'Full AI':'Basic'}</p><p className="app-muted">Report depth is based on your plan.</p></div></div><section className="card app-section"><div className="app-toolbar"><div><h2 className="section-title">Run a website audit</h2><p className="app-muted">Analyze usability, WCAG-aware accessibility, conversion friction, and visual hierarchy.</p></div><Search className="h-8 w-8 text-brand-300" aria-hidden="true" /></div><AnalyzeForm /></section><section className="card app-section"><h2 className="section-title">Top Issues</h2><div className="mt-5 grid gap-3">{[['HIGH','CTA button lacks contrast','Primary CTA may not meet contrast and visibility expectations.'],['HIGH','No clear call-to-action above the fold','Users need a dominant next step immediately.'],['MED','Form is too long','Reduce unnecessary fields and improve completion confidence.'],['LOW','Inconsistent spacing','Tighten layout rhythm for a more polished experience.']].map(([severity,title,copy])=><div key={title} className="issue-row"><div className="issue-row-main"><span className={severity==='HIGH'?'badge badge-high':severity==='MED'?'badge badge-med':'badge badge-low'}>{severity}</span><div><p className="issue-row-title">{title}</p><p className="issue-row-copy">{copy}</p></div></div><ArrowRight className="h-5 w-5 text-brand-300" aria-hidden="true" /></div>)}</div></section></AppShell>; }

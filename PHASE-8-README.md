# Darkstar Audit AI — Phase 8 Roadmap Generator

Phase 8 turns audit findings into an actionable UX roadmap.

## Adds

- AI Roadmap API
- Roadmap generation from saved reports
- Pro-gated roadmap builder
- Roadmap database table
- Roadmap page
- Report-to-roadmap CTA component
- Stakeholder-ready action plan format

## Apply

```bash
cd ~/Desktop/uxiq-auth-clean
unzip ~/Downloads/darkstar-phase-8-roadmap-generator.zip
npm run build
git add .
git commit -m "Phase 8 AI roadmap generator"
git push
```

## Supabase

Run:

```text
supabase/phase8-roadmaps.sql
```

## Environment

Requires:

```env
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
```
